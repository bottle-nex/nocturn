{
  "name": "nocturn",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "db:generate": "cd packages/database && npx prisma generate && cd ../..",
    "remove-logs": "turbo run remove-logs",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "knip": "^5.82.1",
    "prettier": "^3.6.2",
    "prisma": "7.1.0",
    "turbo": "^2.5.4",
    "typescript": "5.8.3"
  },
  "dependencies": {
    "@prisma/client": "7.1.0"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.4"
}
